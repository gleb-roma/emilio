ssh gvroman@corn11.stanford.edu

scp *.m gvroman@corn.stanford.edu:~


screen -ls
screen -S new_name
screen -r  existing_name
<type exit to exit the screen session>

https://sites.google.com/a/stanford.edu/rcpedia/how-do-i/run-a-job-that-continues-after-i-log-out
To leave this virtual session and return to the main session, use control-a d (type control and a at the same time, then d).

module load matlab
krenew -t -- matlab -nodisplay
path(path,'~/MATLAB') 

krenew -t -- matlab -nodisplay -r iter_value_function_and_pi.m 


zip -r vx *.mat
tar -zcvf vx *.mat

cd /Users/gleb/Dropbox/Stanford/RA\ for\ Susan/Emilio/saves/7
scp gvroman@corn.stanford.edu:~/saves/7/vx.zip .
unzip vx
tar -zxvf vx
